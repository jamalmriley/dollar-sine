import React from "react";

export default function Prompt3() {
  return (
    <div>
      <h2 className="h2">Browse courses.</h2>
      <span>
        You can assign courses to students one at a time, by class, or all at
        once.
      </span>
      <h2 className="h2">Assign courses.</h2>
      <span>
        You can assign courses to students one at a time, by class, or all at
        once.
      </span>
    </div>
  );
}
